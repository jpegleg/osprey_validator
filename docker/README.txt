Clone and build the royal_blobs_jwt_service and put the binary in the docker build dir, otherwise the build will fail.

Also please replace any cryptographic components if you actually use this stuff, the files are there for demo purposes.
